<?xml version="1.0" encoding="UTF-8"?>
<b:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:b="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

	<http use-expressions="true">
		<intercept-url pattern="/favicon.ico" access="permitAll" />
		<intercept-url pattern="/login*" access="permitAll" />
		<intercept-url pattern="/error" access="permitAll" />
		<intercept-url pattern="/js/**" access="permitAll" />
		<intercept-url pattern="/css/**" access="permitAll" />
		<intercept-url pattern="/images/**" access="permitAll" />
		<intercept-url pattern="/responsabile/**" access="hasAuthority('RESPONSABILE')" />
		<intercept-url pattern="/direttore/**" access="hasAuthority('DIRETTORE')" />
		<intercept-url pattern="/organizzatore/**" access="hasAuthority('ORGANIZZATORE')" />
		<intercept-url pattern="/**" access="isAuthenticated()" />

		<form-login login-page="/login"
					default-target-url="/"
					authentication-failure-url="/login?error" />

		<logout logout-url="/logout" logout-success-url="/login?logout" />
	</http>

	<!-- TODO: migrate to database -->
	<authentication-manager>
		<authentication-provider>
			<user-service>
				<user name="responsabile" password="{noop}responsabile"
					authorities="RESPONSABILE" />
				<user name="direttore" password="{noop}direttore"
					authorities="DIRETTORE" />
				<user name="organizzatore" password="{noop}organizzatore"
					authorities="ORGANIZZATORE" />
			</user-service>
		</authentication-provider>
	</authentication-manager>
</b:beans>